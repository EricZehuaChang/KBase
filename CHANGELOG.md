# CHANGELOG

## v1.0.0（2026-07-15）—— 首个产品版本

**质量基线**：后端 pytest 449 passed（external/pg 按设计跳过）；前端 vitest 107 passed + vue-tsc 构建通过；核心能力均经浏览器端到端实测。

### RAG 核心链路

- 摄取三条解析路（上传时可选）：`auto`（markitdown + 扫描件自动路由 GLM-OCR）、`ocr` 表格增强（文本层 PDF 强制结构化解析，跨页断表合并）、`vlm` 满血视觉深度识别（识别结果人工校验确认后才向量化，防幻觉入库）
- 表格全套：原子分块（永不切碎）+ 大表按行组切分重复表头 + 行线性化（"列=值"进向量与 BM25）+ 跨页断表合并
- 父子分块 + 双路召回（向量 + BM25 关键词）+ RRF 融合 + 重排（可降级）+ 小到大上下文组装
- 检索策略三层可编排：全局默认 → KB 级用法开关 → 请求级试跑覆盖
- 引用溯源：文本层 PDF 页码定位 + 原文件内联预览跳页；回答附命中页文档插图（多模态一期，图片不进 prompt 零幻觉）；命中即溯源（表格/图片定位到对应对象，不做 bbox 高亮——产品决策）

### 模型接入（全部页面配置化）

- LLM Provider：页面增删改查 + API Key 页面维护（脱敏显示）+ 连通性测试 + 各厂商模型目录拉取（7 天缓存周更）；预置智谱/通义/DeepSeek/Kimi/硅基流动/OpenAI 及企业自有 OpenAI 兼容平台
- 向量模型：KB 级绑定（建库选择）+ 密钥页面维护（DB 覆盖 > 环境变量）+ admin 换绑触发全库重建（存量文本重嵌入，零 OCR 费用）
- OCR：GLM-OCR（智谱 layout_parsing）；VLM 深度识别 provider 可配

### 企业能力

- 认证鉴权：本地账号（viewer/editor/admin 角色矩阵）+ API Key + 企业 SSO（标准 OIDC 授权码流，默认关闭）
- 库级权限 ACL："不配公开、一配收紧"（授权用户 + 建库人 + admin）
- 多库联合问答：会话绑定多库，散射-聚合跨库检索，引用带来源库
- OpenAI 兼容 API：每个知识库暴露为 `/v1/chat/completions` 的"模型"，生态客户端零改造接入
- 反馈闭环：回答点赞点踩 + 差评清单（带问题原文）入运营看板
- 运营看板：问答量/拒答率趋势 + 无答案问题清单 + 赞踩统计
- 评测回归：评测集 + 一键回归（hit@k / MRR，只评检索零 LLM 费用）+ 历史对比
- URL 连接器：网页地址直接导入（内网 wiki/门户）
- Chunk 运营：分块启停（索引摘除可恢复）+ 文本编辑重嵌入

### 部署与运维

- 双形态：lite（SQLite+Chroma+进程内 bge-m3，POC/小团队）/ standard（PostgreSQL+Qdrant+TEI，生产/较高并发）。**容量口径**：仓库内唯一的压测是 standard 档 40 篇合成文档 / 357 个可检索单元、100 并发，检索 P95 约 4.7s（调优前）～9.3s（调优后），**未达 500ms 验收线**（见 `loadtest/report-standard.md`）；"十万级文档"属单位参数实测 + 容量规划外推，不是实测结论，**lite 档不支持十万级**
- 备份恢复：`scripts/backup.py`（SQLite 在线快照不停机备份、恢复可回滚）+ 运维手册（备份剧本/升级五步/监控接入）
- 可观测：`/healthz` + `/metrics`（Prometheus）
- 批量导入：`kbase/bulk_import.py` CLI（清单驱动断点续传 / 失败重跑 / `--workers` 并发 / `--parse-mode`；"十万级灌库"仅为容量规划外推——该工具的端到端测试最大规模是 30 个文件，`lite` 档不支持十万级）
- 许可证：Ed25519 验签，试用/到期前 30 天/过期三级横幅提醒（不锁功能）
- 演示数据一键装载（幂等）

### 前端

- 双 SPA：使用端（问答/会话/多库联查/反馈/插图灯箱）+ 管理端（Vben 风骨架：分组导航/激活指示条/面包屑，统一 PageHeader）
- 上传进度条（XHR）、暗色模式、角色门控

---

## v1.0.x（2026-07-15，v1.0.0 封版当日增量）

- **新功能**（2026-07-15）docx 插图 caption 级锚定：解析 document.xml 按章节标题锚图，引用命中该章节才附图（不做全文档洪泛）；图片文件文档（扫描照）命中即附原图。attach_images 三路锚点（PDF 页级 / docx 章节级 / 图片文档原图）。后端 451 绿
- **体验优化**（2026-07-15）使用端聊天页 UI 专项：会话列居中限宽、accent 用户气泡、卡片式输入区（内嵌发送钮+合规提示行）

---

## v1.0.x 后续增量（2026-07-16 起）

> **版本口径**：仓库当前只有 `v1.0.0` 一个 tag（`git tag -l`），v1.0.0 之后**没有发过 v1.1.0 或任何新 tag**。以下为 2026-07-16 起落在 `main` 上的增量，按提交日期归集；每条日期取自对应提交的提交日期（`git log --date=short`）。每条只用 `新功能` / `体验优化` / `问题修复` / `企业能力` 四个标签中的一个。
>
> **本文件核对基准**：`main` 的 `8e6f2eb`（2026-09-14，09-14 缺口修复轮 T01–T07 收官）。该基准之后落在 `main` 上的提交不在本文件覆盖范围内；因仓库仍在推进，引用本文件时请一并确认当时 HEAD。

### 2026-07-16 ~ 2026-07-21（飞书连接器 · 账号与邮件 · 免登录分享 · PDF 解析器）

- **新功能**（2026-07-16）飞书知识库连接器一期：设置页新增「飞书连接器」卡片维护 App ID / App Secret（回显只给密钥尾 4 位），库详情页「从飞书导入」对话框支持粘贴 wiki 链接导入该子树、或按 space_id 导入整个空间；每个 wiki 节点导入为独立 Markdown，wiki 路径注入标题链，`heading_path` 呈现「空间路径 > 文档 > 文内章节」；未配凭据返回 409
- **问题修复**（2026-07-16）飞书导入失败不再一闪而过、0 篇成功不再误报成功：错误改为对话框内常驻红条并带「共发现 N 篇 / 正文全部拉取失败 / 首条原因（截 160 字）」，附三条高频原因排查（应用需发布版本、应用需加为知识库成员、链接需是 `/wiki/` 地址），失败落审计 `feishu_import_failed`
- **体验优化**（2026-07-16）飞书配置三步指引常驻界面（建应用 → 开通权限并创建版本发布 → 加为知识库成员），权限一次开全六项只读——`wiki:wiki:readonly` / `docx:document:readonly` / `drive:drive:readonly` / `docs:doc:readonly` / `sheets:spreadsheet:readonly` / `bitable:app:readonly`，并附按 app_id 动态生成的「一键开通」链接
- **新功能**（2026-07-16）飞书文档图片同步：文档内图片按所在章节标题锚入插图体系（落 `document_images` 表，与 PDF / docx 同一套表、同一套过滤与附图上限、同一灯箱），问答命中该章节即附图
- **问题修复**（2026-07-16）飞书文档内嵌素材图片下载 400：`GET /drive/v1/medias/{media_token}/download` 对 docx 素材必须带 `extra.drive_route_token` 来源声明
- **新功能**（2026-07-16）账号邮箱字段（建号可填、用户列表显示、编辑可改可清空）+ 登录用户自助修改密码（当前密码必填，旧密码错回 401）
- **新功能**（2026-07-16）发件箱设置（SMTP）：管理端「发件箱」卡片维护主机 / 端口 / 用户 / 密码或授权码（留空 = 保留旧值）/ 发件人地址与显示名 + 测试邮件按钮，配置存 `app_settings`（`smtp_host` 等，非环境变量）；建号时若填了邮箱且发件箱已配置则自动发账号开通通知
- **新功能**（2026-07-16）忘记密码邮箱重置（一次性 token 存 `app_settings`，TTL 30 分钟，接口统一响应防账号枚举）+ 首登邮箱引导（可「稍后再说」）+ 自助绑邮箱
- **问题修复**（2026-07-16）回答气泡渲染行内 Markdown：`**加粗**` 不再露出星号
- **体验优化**（2026-07-16）设置页分栏重构：五组左侧子导航替代单页长滚动
- **问题修复**（2026-07-16）库卡片文档数 0→N 跳变：`doc_count` 随库列表一次返回
- **问题修复**（2026-07-16）管理端点击库卡片白屏：`openKb` / `backToGrid` 仍指向已废弃的 `/kb` 路由
- **问题修复**（2026-07-16）快捷问题按当前库真实文档动态生成，废除写死的旧评测语料
- **问题修复**（2026-07-16）lite Compose 透传 `HF_HUB_OFFLINE`：模型权重进卷后跳过启动期 Hugging Face 在线校验（hf-mirror 504 卡启动）
- **新功能**（2026-07-18）KBase 品牌邮件模板：账号开通 / 密码重置 / 发件箱测试
- **问题修复**（2026-07-18）飞书深层级文档问答附图恒空：结构分块器的标题识别从 `#`~`####` 扩到 Markdown 全部六级（h1–h6），被 wiki 祖先链下推到 h5/h6 的文内章节能进 `heading_path`
- **问题修复**（2026-07-18）首次登录知识库下拉为空：顶栏加载闩锁失败时不回滚
- **体验优化**（2026-07-19）文本层 PDF 主解析器切换 OpenDataLoader：真实标题层级 / 阅读序 / 边框表格进标题感知分块，页码定位改用与 markdown 同一次解析的 JSON 同源页文本，CJK 断行空格清洗保 FTS5 精确匹配，全标题退化产物降级为段落（防结构分块器零叶子块导致全文不可检索）。镜像补装 `default-jre-headless`（需 Java 11+），包缺失 / 无 Java / JVM 失败一律回退 markitdown，`KBASE_PDF_PARSER=markitdown` 为硬开关。**如实说明**：JRE 体积增量「约 +250MB」是提交正文与 Dockerfile 注释的声称值、无实测记录；Java 可用性探测原本用 `shutil.which("java")`，到 2026-09-08 的 CI 提交才改为真执行 `java -version` 看退出码（macOS 无 JDK 时 `/usr/bin/java` 是存根，会误判为可用）
- **新功能**（2026-07-19）免登录分享 + 嵌入 widget：`share_links` 表与 editor 管理端点（建链接 / 列表 / 撤销），公开端点 `GET /api/share/{token}`、`POST /api/share/{token}/query`（复用登录端同一问答编排，引用 / 附图 / 拒答语义一致），极简分享页 `/share/{token}` 支持 `?embed=1`，`widget.js` 一行 script 可嵌任意网站（右下浮动问答球 + 懒加载 iframe）；新增 `users.advanced_ui` 按用户控制 viewer 顶栏模型选择 / 多库联查可见性
- **问题修复**（2026-07-19）分享页免登录直达被 401 拦截器劫持到登录页（会话探测跳过 `/share/` 前缀）；分享页附图 401 裂图——新增公开图片端点 `GET /api/share/{token}/images/{doc_id}/{filename}`
- **新功能**（2026-07-19）飞书群机器人：群 / 单聊 @机器人 提问经公开回调 `POST /api/feishu/events` 受理，检索 + 生成放 BackgroundTasks，以 interactive 卡片回复（答案 + 引用来源前 3 个文档）；设置页维护 verification token / encrypt key / 绑定知识库 / 回答模型，`event_id` 进程内 LRU 去重，回答落审计 `feishu_bot_answer`
- **问题修复**（2026-07-19）飞书附图精准化三合一：图片 / 画板锚由叶子标题升级为标题链（不再因跨模块同名叶子乱配图），正文落「（图：X）」文字标记使纯图章节可被检索，新增飞书画板（board）块导出为图片；另加附图洪泛防线，单条引用附图上限 `MAX_IMAGES_PER_CITATION=4`
- **问题修复**（2026-07-19）删除文档 / 删除库时级联清理 `document_images` 行：该表无 FK 级联，此前只删向量 / FTS / chunk / document 行与 files 目录，图片索引行成孤儿死行持续积累
- **问题修复**（2026-07-19）插图标记的生成引导：用户在正文里问「图长什么样」不再答「资料不足」，改为指引查看下方插图（仍禁止凭空描述图片内容）
- **新功能**（2026-07-20）连接器框架 + 定时增量同步：新增 `connectors` / `connector_docs` 两表，飞书导入升级为首个正式连接器——`interval_minutes` 定时间隔（0 = 仅手动）、`enabled` 启停（停用只跳过调度，手动「立即同步」不受影响）、`prune` 镜像源侧删除、`last_sync_*` 状态；双指纹增量（`obj_edit_time` + markdown sha256，指纹变而内容未变只刷指纹不重摄取）；`ConnectorScheduler` 守护线程每 60s tick 串行同步，DB 抢锁防多进程双跑；前端连接器对话框提供列表 / 新建 / 启停 / 立即同步 / 删除
- **新功能**（2026-07-20）多库联查库级权重 `union_weight`：各库分数乘以其权重后再全局排序（0.1~10，缺省 1.0 = 既有行为；库配置对话框可配）
- **体验优化**（2026-07-21）飞书群机器人卡片内置一键授权链接（`im:message` + `im:message:send_as_bot`），与文档导入对话框对称；未配应用凭据时不渲染链接并提示先去连接器配凭据

### 2026-07-22 ~ 2026-07-27（多语言 · 超级管理员与自定义角色 · 会话策略）

- **新功能**（2026-07-22）多语言支持：Vue I18n 接入双 SPA，界面语言清单单一事实源为 `web-app/src/i18n/languages.ts`，内置中文（默认 + 全量源与回落）/ English / Bahasa Melayu 三语；语言判定优先级为账号偏好 > `localStorage`(`kbase-lang`) > 浏览器语言前缀 > 中文，缺译文一律回落中文；当前基线为 780 键 / 语言（三语 key 集合完全一致，有 `locale-consistency` 测试防漂移），机翻脚本 `scripts/i18n_machine_translate.py` 以 zh 为源补缺
- **新功能**（2026-07-23）管理端「多语言」翻译管理页（`/admin/translations`）：`translations` 覆盖表按 `(lang, key)` 覆盖前端基线译文（DB 优先，空表 = 全用基线，值传空串 = 删覆盖行回落基线），管理员可在线改文案并即时生效；`GET /api/i18n/{lang}` 为公开端点（分享页要用），`GET / PUT /api/i18n` 为 `require_admin`；页面支持按 key 或任一语言译文搜索、按 key 首段筛选、「只看缺译」、已改 / 缺译角标；加新语言零改后端
- **体验优化**（2026-07-23）使用端与管理端全量组件键化抽取，后端错误消息 key 化全覆盖：`AppError(code, message, params)` 统一返回 `{detail: {code, params, message}}`，前端按当前语言渲染，未 key 化的旧端点回落中文原串（提交态 60 个 code 全部存在于 zh 基线）
- **体验优化**（2026-07-23）账号级语言偏好（`PUT /api/auth/language`，用户菜单选语言即写库、跨设备一致）+ 飞书三处权限指引键化
- **问题修复**（2026-07-23）飞书 `url_verification` 握手不再依赖 KBase 是否已配置：无条件回 challenge，未配 verification token 的 403 下移到握手之后、只对真实业务事件生效——消除「先验证回调地址、后填 token」的顺序死锁
- **新功能**（2026-07-24）接入 Kimi `kimi-k3` 模型（推理档，`reasoning_effort=high`），密钥走 `MOONSHOT_API_KEY`，lite Compose 已透传
- **问题修复**（2026-07-24）复制按钮在 http 演示机失效：抽出 `copyToClipboard` 兼容工具并全站接入（非 HTTPS 环境 `navigator.clipboard` 不可用）
- **新功能**（2026-07-24）免登录分享支持多库联查：建链接收 `extra_kb_ids`（副库逐一校验，脏引用 404），`share_links.kb_ids` JSON 列（NULL = 单库旧行为），匿名问答在存活绑定库 >1 时走 `retrieve_multi` 散射聚合，附图端点放行全部绑定库；主库删 = 链接失效、副库删 = 静默缩范围
- **体验优化**（2026-07-24）顶栏语言钮带当前语言母语名；登录页语言切换改为页脚母语名文字行（替换角落悬浮地球图标）
- **企业能力**（2026-07-26）引入超级管理员层级：`superadmin` 在管理体系之外，普通 admin 既看不到也管不到超管账号（用户列表直接过滤掉超管行；改 / 删超管按「不存在」回 404 而非 403，避免泄漏账号存在性，与全仓不泄露存在性的 404 语义一致）；首启引导账号即 superadmin
- **企业能力**（2026-07-26）审计分层：超管操作记录仅超管本人可见（审计、问答统计一并分层）
- **新功能**（2026-07-26）管理端审计日志可视化卡片 `AuditLogCard.vue`：`GET /api/audit` 首次获得前端入口（概览 chips + 近 14 天趋势 + 操作者 / 动作筛选与「只看我」+ 粘头明细表 + 分页续载）。记录面：53 个 mutating 端点自动落 `{METHOD} {path_template}`，另有 10 个显式动作（登录成功 / 失败、忘记密码、重置密码、问答、拒答、分享问答、飞书机器人回答、飞书导入失败等）；被 403 拒绝的请求不落审计
- **企业能力**（2026-07-26）超管全量资料维护：可改任意账号名 + 删除账号（含私有数据级联）
- **企业能力**（2026-07-26）用户「邮箱与邀请」：维护邮箱 + 发送凭据邀请邮件（`email_templates.py` 的 `account_invite`，含登录地址 / 账号 / 初始密码，按账号语言选中英文模板）；同步发送、发信成功才落库，未配发件箱回 422、发信失败回 502。**边界（如实）**：邀请发信即重置该账号密码
- **企业能力**（2026-07-26）会话策略：默认会话级 Cookie（`kbase_session`，不设 `max_age`，关浏览器即清除、重开必须重新登录），JWT 自身有效期从 7 天放宽到 30 天兜底（`SESSION_TOKEN_TTL_SECONDS = 30*24*3600`，浏览器一直开着的长工作会话不会被中途登出）；登录时可选「记住登录」，签发 30 天持久 Cookie（`max-age=2592000`）。**如实说明**：SSO / OIDC 回调至今只发会话级 Cookie、不认「记住登录」
- **体验优化**（2026-07-26）顶栏只留登录人，语言 / 主题 / 改密 / 登出收进用户菜单下拉，语言选择改经典二级菜单模式
- **企业能力**（2026-07-27）自定义角色：超管手工建角色、勾权限，权限真实强制。权限词表 `content.manage`（内容管理）/ `system.admin`（设置组）/ `audit.view`（审计与运营看板）；内置四角色映射为权限集合（viewer = 空集，editor = 内容，admin = 全部），自定义角色存 `roles` 表（内置不落库），删除角色后仍挂着它的用户回落空集 = 只剩问答。**边界（如实）**：`audit.view` 目前**没有任何路由消费**——`/api/audit` 与 `/api/stats/*` 仍挂 `require_admin`，只勾 `audit.view` 的角色访问审计会 403，即提交信息所称的「只看审计不碰设置」的审计员角色尚未真正落地

### 2026-08-02 ~ 2026-08-06（产品导览 · 登录重设计 · 方案卡三件套）

- **新功能**（2026-08-02）产品导览：15 步「下一步」式功能巡礼（跨使用端 / 管理端整页跳转并续步，键盘 ←→ 翻步，Esc 收起为胶囊），导览卡可拖拽（位置存 `localStorage`）；15 步中 13 步配自动演示——逐字填示例问题（真派发 `input` 事件）、程序化点击锚点、`scrollIntoView` + 脉冲高亮（发送按钮只高亮、不代点）；入口按账号白名单显示（AppSetting `tour_allowed_users`，超管恒可见）。**边界（如实）**：该白名单没有管理 UI 也没有写入端点，只能直接改 DB
- **问题修复**（2026-08-02）密码重置邮件链接落不到重置表单：链接由根路径 `/?reset_token=` 改为 `/login?reset_token=`，并在路由守卫加兼容分支（任意路径带 `reset_token` 即原样转交 `/login`，已发出的旧链接同样可用）
- **体验优化**（2026-08-02）VLM 校验对话框重设计为「校对工作台」：独立头部 + 双栏主体（左「原图」带缩放工具条 0.25x~4x，右「识别结果」可直接编辑）+ 底部操作栏；右下角手柄拖拽改窗口大小（下限 560×420，尺寸跨次保留），中缝拖拽调分栏比例 25%~75%。**如实说明**：可拖的是缩放手柄与分栏中缝，**没有**拖动窗口位置的能力（提交标题的「窗口可拖拽缩放」指前者）
- **新功能**（2026-08-02）回答语言跟随提问：`SYSTEM_PROMPT` 明确「回答语言始终与提问语言一致，资料语言与提问语言不同不构成资料不足，用户明确要求则以用户要求为准」；不经 LLM 的拒答文案也按提问语言启发式裁决。另增系统邮件语言配置 `smtp_language`（`auto` 跟随收件人账号语言，或强制 `zh` / `en`）
- **体验优化**（2026-08-02）Provider 厂商徽章：问答顶栏模型选择与管理端模型服务卡片显示厂商官方矢量 logo（lobe-icons MIT 静态 SVG，`web-app/src/assets/vendors/` 随包内置、离线可用，当前 6 家）；未知厂商回落品牌色 + 缩写的中性徽章
- **新功能**（2026-08-03）登录页高级重设计（cobe 3D 地球 + 玻璃卡、单一深色世界）+ 首页 3D 氛围与入场动效
- **问题修复**（2026-08-03）备份不再打包 `data/backups` 自身：历史备份被递归打包致体积几何级膨胀（演示机 1.1G→8.7G），新增排除断言测试
- **新功能**（2026-08-06）方案卡三件套 + API Key 库白名单：①Markdown YAML front matter 元数据摄取（仅 `.md` / `.markdown`，首行须为 `---` 且有闭合行；解析失败绝不阻塞摄取，正文照常分块），随块落 `chunks.meta` 与向量库 payload；②检索 `filters` 元数据过滤（扁平 dict，字段间 AND、列表内 OR），MCP 工具 `search_knowledge` / `ask_knowledge_base` 同步新增 `filters` 参数，工具说明可用 `KBASE_MCP_FILTERS_DOC` 追加业务元数据词表；③API Key 库级白名单 `scope_kb_ids` 服务端强制——越权检索静默返回空集、越权问答返回与「检索无依据」完全一致的拒答流（不报错，防探测），`/api/kb` 只列白名单库。**边界（如实）**：管理端新建 API Key 表单只提交名称与角色，暂无库选择入口；本提交当时该白名单**未覆盖 `/v1` 兼容接口**，2026-09-14 已补齐（见下）

### 2026-09-02 ~ 2026-09-14（结构化参数检索器 · 中转模型 · CI 门禁 · 09-14 缺口修复轮）

- **新功能**（2026-09-02）结构化参数检索器 P0：新增 `kbase/params.py` 作为唯一权威——单元格数值解析 + 量纲归一 + 行组参数区间抽取，Qdrant / Chroma / BM25 后过滤三条检索路共用同一组范围判定函数（三路一致性契约测试钉住）；「不猜」原则——无单位裸数字、未知量纲、列覆盖率 <60%、同列混量纲一律整列不抽取。同时修复 `reindex` 重建时 payload 未铺文档级 front matter、导致重建后稠密路 filters 静默失效的既有缺陷。**边界（如实）**：参数范围匹配是块级 min/max 区间相交，不是逐行精确筛选；本提交当时 HTTP 请求模型只接受标量与标量列表，范围条件走 `SearchBody` / `QueryBody` 仍返回 422——该 HTTP / MCP 契约已在 2026-09-14 补齐（见下）
- **问题修复**（2026-09-02）撤回 `chromadb<1` 依赖上界：线上跑的就是 1.5.9，钉上界会导致降级；测试环境单独装 `chromadb<1` 以规避 1.x 在测试进程内反复创建 client 的线程膨胀
- **新功能**（2026-09-04）接入 4 个 iruidong 中转通道：`iruidong-glm-flash`（glm-5.3-flash）/ `iruidong-ds-flash`（deepseek-v4-flash）/ `iruidong-ds-pro`（deepseek-v4-pro）/ `iruidong-glm`（glm-5.3），OpenAI 兼容，密钥走 `IRUIDONG_API_KEY`。**如实说明**：本提交当时该变量**没有**透传进容器（两份 Compose 均未列），2026-09-14 已补齐（见下）；iruidong 无厂商 logo，徽章回落中性「API」块
- **企业能力**（2026-09-08）GitHub Actions 三层接入：`ci-core.yml` 四个 job（ruff 最小集 + 配置可加载 / pytest / PG 16 集成 / 前端 vitest + build + `web/` 产物漂移检查），`ci.yml`、`docker-ci.yml`（main 镜像构建与冒烟）、`nightly.yml`（Chroma 1.5.9 金丝雀、依赖审计、每日镜像、全量回归）、`release.yml`（`v*` tag 门禁 + `git archive` 源码打包挂 Release）、`docker-build.yml`；另补 `scripts/check_config.py`、`.gitattributes` 与 `web-app/.nvmrc=22`。并修复 docker 冒烟必须显式 `--entrypoint`（默认走 `entrypoint.sh` 会 `exec uvicorn` 启动真应用，单步挂 26 分钟）与 `web/index.html` 行尾统一为 LF（修 Windows 提交带入的 CRLF 跨平台构建漂移误报）
- **问题修复**（2026-09-08）Chroma 并发问题两处：①`get_or_create_collection` 并发首用竞态——并行摄取偶发 `UniqueConstraintError(Collection already exists)`（约 10% 概率一份文档 `failed`），改为 per-client 锁内串行化取 / 建集合；②jieba 词典懒初始化与并发 `cut` 的时序窗口，改为模块导入即初始化
- **问题修复**（2026-09-14）`/v1` 兼容入口应用 API Key 库白名单：此前用真实鉴权依赖 + 内存合成数据可复现「只授权 A 库的 viewer key 能列出并访问白名单外的库」；修复后 `/v1/models` 与 `/v1/chat/completions` 与原生路径同口径
- **问题修复**（2026-09-14）文档 / 分块 / 评测 / 任务 / 连接器 / 分享链接的库级 ACL 与 scope 守卫：新增 `kbase/api/guards.py` 收口判定，资源型端点越权回 404（与「资源不存在」不可区分）、检索 / 问答路径静默空集（防探测），并补 449 行权限矩阵测试
- **问题修复**（2026-09-14）数值范围 filters 打通 HTTP 公共入口：`filters` 现在接受范围条件 dict（`{"gte":450,"lte":550}` 或 `{"approx":500,"tol":0.1}`），并对未知键、非数值、上下界颠倒、`tol` 为负、`approx` 与 `gte`/`lte` 混用逐项给出明确报错；MCP 同步可用
- **新功能**（2026-09-14）`reindex` 为旧表格块补算参数区间并落库：`python -m kbase.reindex --kb <id>` 默认补算，`--no-backfill-params` 关闭；已有 params 的块不动（人工校正值不被覆盖），且只对仍是合法 Markdown 表格的块生效
- **问题修复**（2026-09-14）standard 档 PG 密码走环境变量渲染：`db.password_env` 指名环境变量，`resolve_db_url` 用 `make_url().set(password=...)` 渲染（含 `@ : / % {` 的密码自动转义），变量缺失直接报错而非静默用错密码连库
- **问题修复**（2026-09-14）lite 与 standard 两份 Compose 透传 `IRUIDONG_API_KEY`；新增受跟踪的 `.env.example`（只写变量名与说明、不含真实值），并由 `scripts/check_env_example.py` 在 CI 核对 compose / config 引用的变量是否齐全
- **问题修复**（2026-09-14）交付包缺口：镜像与 `release.yml` 的 `git archive` 白名单带上 `scripts/` 与文档（此前只拿到运行包的人无法照着文档执行 `scripts/backup.py`，也拿不到手册）；`docs/manual/运维手册.md` 的健康 / Prometheus 示例端口口径统一为 8100

---

## 后续计划（Roadmap，按优先级）

> 本节按 `main` 的 `8e6f2eb`（2026-09-14）代码核对结果更新：已完成的划掉并注明依据，未完成的注明核对结论。

1. **多模态二期·视频**：ASR 语音转写插件槽（本地 Whisper / 云 API 配置化）→ 带时间戳分块 → 命中后播放器 seek 到时间点（与 PDF 页码跳转同构）；增强路线：关键帧 + VLM 描述。**未开工**——全库无 ASR / Whisper / ffmpeg 相关代码
2. **SSO 落地联调**：对客户真实 IdP（Keycloak / Authing / Azure AD）跑通一次授权码流。**仍未做**——OIDC 实现与测试随 v1.0.0 交付，仓库内没有真实 IdP 联调证据
3. **工程健康**：`@app.on_event` → lifespan 迁移（低收益，连同 test_api 断言一起改）。**仍未做**——`kbase/api/main.py` 仍是 `@app.on_event("startup" / "shutdown")`，连接器调度器的启停也挂在上面
4. **部署侧**：DashScope 首次 TLS 握手 5-8s 的生产网络复核；媒体目录（图片 / 未来视频）备份策略与 `data_dir` 分离。**部分完成**——备份脚本已修掉「递归打包 `data/backups` 自身」的体积膨胀缺陷（2026-08-03），交付包已带上 `scripts/` 与文档（2026-09-14）；媒体目录分离与 TLS 复核仍未做
5. ~~**合并回主仓**：`feature/m5-1` → 主分支时机由项目 owner 决定~~ **已完成**：`feature/m5-1` 已是 `main` 的祖先（`git merge-base --is-ancestor feature/m5-1 main` 通过），v1.0.0 之后的开发全部落在 `main`

### 仍存在的缺口（按优先级，核对基准 `8e6f2eb`）

| 优先级 | 当前缺口 |
|---|---|
| P0 | 仍缺 API Key 生命周期治理：没有过期时间、最近使用时间、IP 白名单与配额（`api_keys` 表只有 8 个字段）；`/v1` 响应的 token 用量恒为 0；全仓没有限流实现 |
| P0 | 分享链接没有过期 / 密码 / 访问次数上限；登录失败只有审计、没有锁定与退避 |
| P1 | 渠道接入只有飞书一个点，且**未映射的渠道用户只能访问公开库**（收紧过的库一律拒答）——这是设计取舍不是缺陷，但接入企微前需先确认身份映射方案（T19 已提供 `channel_identities` 与适配层） |
| P1 | 问答结果无归因埋点：拒答只留一条 100 字审计，`/v1` 与飞书机器人路径各写各的；运营看板无法按空检索 / 低于阈值 / 越权分桶 |
| P2 | 包版本仍是 `0.1.0`（前端 `0.0.0`）；Python 依赖为版本范围，`qdrant/qdrant`、TEI 使用 `latest` 类标签，尚未形成固定 digest 的物料清单 |

已闭环（09-14 缺口修复轮 T01–T07，均有新增测试钉住）：`/v1` 入口的库白名单、文档 / 分块 / 评测 / 任务 / 连接器 / 分享链接的库级 ACL 与 scope 守卫、数值范围 filters 的 HTTP 契约、旧表格参数回填、standard 的 PG 密码环境变量渲染、`IRUIDONG_API_KEY` 透传与 `.env.example`、发版包与镜像带上 `scripts/` 与文档、运维手册端口口径统一为 8100、CHANGELOG 补齐至 2026-09-14。

### 其他必须如实说明的边界

- 库级 scope 对检索 / 问答路径是**静默空集**（不报错、不提示，防探测）；对文档 / 分块 / 评测 / 任务 / 连接器 / 分享链接等资源型端点是 **404**（与「资源不存在」不可区分）。这两套语义是有意区分的，不要按「统一报错」改。
- standard 档的真机链路（PG 连库、Qdrant、TEI、5 服务全栈启动）**未验证**：核对时本机没有 Docker daemon 与 standard 栈。
- 真实 IdP 联调、更多连接器与源权限同步、视频 / ASR 时间戳、Deep Research 与编排属于后续方向，**尚未上线**，不应作为现成能力对外承诺。
- 引用溯源不做逐页 bbox 高亮，这是已记录的产品取舍，不算遗漏项。
- 压测数字（10 并发 P95 亚秒、100 并发 P95 约 4.7s～9.3s）来自特定机器上的一次实测，其中 100 并发**未达** 500ms 验收线；详见 [`loadtest/report-standard.md`](loadtest/report-standard.md)，不能当作当前环境 SLA。
