# KBase M3-2（MCP Server）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 独立 MCP 进程（STDIO + Streamable HTTP）暴露 list/search/ask 三工具，HTTP 反调 KBase API。

**Architecture:** `kbase_mcp/` 顶层包：`server.py`（FastMCP 实例与三工具，httpx.AsyncClient 注入）+ `__main__.py`（传输选择与 CLI）。测试用 ASGI 内存传输直连 create_app(fakes)。

**Tech Stack:** 官方 `mcp` SDK（pyproject 可选组 `[mcp]`），httpx（已有）。

**Spec:** `docs/superpowers/specs/2026-07-05-kbase-m3-2-design.md`
**基线：** main @ ff1a3a6（107 backend / 34 frontend）。分支 `feature/m3-2`。约定沿前例。

---

### Task E1: 工具实现（TDD，ASGI 内存应用）

**Files:** Create `kbase_mcp/__init__.py`（空）、`kbase_mcp/server.py`；Modify `pyproject.toml`（`mcp = ["mcp>=1.2"]` 可选组）；Test `tests/test_mcp_tools.py`

- [ ] Step 1 失败测试（关键用例，完整写出）：

```python
# tests/test_mcp_tools.py
import httpx
import pytest

from kbase_mcp.server import KBaseClient, ask_knowledge_base_impl, \
    list_knowledge_bases_impl, search_knowledge_impl
from tests.test_api import CFG, MD, FakeLLM


@pytest.fixture
def kbase_app(tmp_path, fake_embedder):
    from kbase.api.main import create_app
    cfg = tmp_path / "kbase.yaml"
    cfg.write_text(CFG.format(data_dir=str(tmp_path / "data").replace("\\", "/")),
                   encoding="utf-8")
    return create_app(config_path=cfg, embedder=fake_embedder,
                      llms={"fake": FakeLLM()}, reranker=False)


@pytest.fixture
async def client(kbase_app):
    transport = httpx.ASGITransport(app=kbase_app)
    async with httpx.AsyncClient(transport=transport,
                                 base_url="http://kbase.test") as c:
        yield KBaseClient(c)


async def _seed_kb(client: "KBaseClient") -> str:
    r = await client.http.post("/api/kb", json={"name": "政策库"})
    kb_id = r.json()["id"]
    files = {"files": ("补贴办法.md", MD.encode("utf-8"), "text/markdown")}
    await client.http.post(f"/api/kb/{kb_id}/documents", files=files)
    return kb_id


async def test_list_knowledge_bases(client):
    kb_id = await _seed_kb(client)
    out = await list_knowledge_bases_impl(client)
    assert any(k["id"] == kb_id and k["name"] == "政策库" for k in out)


async def test_search_knowledge(client):
    kb_id = await _seed_kb(client)
    q = "补贴办法.md > 补贴办法 > 第一章 申领条件\n连续工作满两年可申领住房补贴。"
    out = await search_knowledge_impl(client, kb_id, q, top_k=3)
    assert out and "连续工作满两年" in out[0]["text"]
    assert set(out[0]) == {"doc_name", "heading_path", "text", "score"}


async def test_ask_knowledge_base(client):
    kb_id = await _seed_kb(client)
    q = "补贴办法.md > 补贴办法 > 第一章 申领条件\n连续工作满两年可申领住房补贴。"
    out = await ask_knowledge_base_impl(client, kb_id, q)
    assert "满两年" in out["answer"]                     # FakeLLM 输出拼接
    assert out["citations"] and out["citations"][0]["doc_name"] == "补贴办法.md"


async def test_api_unreachable_clear_error():
    dead = httpx.AsyncClient(base_url="http://127.0.0.1:1")   # 无监听端口
    out = await list_knowledge_bases_impl(KBaseClient(dead))
    assert isinstance(out, dict) and "KBase 服务" in out["error"]
    await dead.aclose()
```

- [ ] Step 2 红。Step 3 实现 `kbase_mcp/server.py`：

```python
"""KBase MCP Server：把知识库暴露为 MCP 工具。
通过 HTTP 反调运行中的 KBase API（默认 http://localhost:8100），
不直接加载内核——避免模型双份驻留。"""
import json
import os
from dataclasses import dataclass

import httpx
from mcp.server.fastmcp import FastMCP

DEFAULT_API = os.environ.get("KBASE_API_URL", "http://localhost:8100")
_UNREACHABLE = ("KBase 服务不可达（{url}）。请先启动：uvicorn --factory "
                "kbase.api.main:create_app --port 8100")


@dataclass
class KBaseClient:
    http: httpx.AsyncClient


def _err(url: str) -> dict:
    return {"error": _UNREACHABLE.format(url=url)}


async def list_knowledge_bases_impl(c: KBaseClient):
    try:
        r = await c.http.get("/api/kb")
        r.raise_for_status()
    except httpx.TransportError:
        return _err(str(c.http.base_url))
    return [{"id": k["id"], "name": k["name"]} for k in r.json()]


async def search_knowledge_impl(c: KBaseClient, kb_id: str, query: str,
                                top_k: int = 5):
    try:
        r = await c.http.post(f"/api/kb/{kb_id}/search",
                              json={"query": query, "top_k": top_k},
                              timeout=120)
        r.raise_for_status()
    except httpx.TransportError:
        return _err(str(c.http.base_url))
    except httpx.HTTPStatusError as e:
        return {"error": e.response.text}
    return [{"doc_name": b["doc_name"], "heading_path": b["heading_path"],
             "text": b["text"], "score": b["score"]}
            for b in r.json()["blocks"]]


async def ask_knowledge_base_impl(c: KBaseClient, kb_id: str, question: str,
                                  provider: str | None = None):
    body = {"question": question}
    if provider:
        body["provider"] = provider
    answer, citations = [], []
    try:
        async with c.http.stream("POST", f"/api/kb/{kb_id}/query",
                                 json=body, timeout=300) as r:
            r.raise_for_status()
            event = ""
            async for line in r.aiter_lines():
                if line.startswith("event:"):
                    event = line[6:].strip()
                elif line.startswith("data:"):
                    data = line[5:].lstrip(" ")
                    if event == "token":
                        answer.append(data)
                    elif event == "citations" and data:
                        citations = json.loads(data)
    except httpx.TransportError:
        return _err(str(c.http.base_url))
    except httpx.HTTPStatusError as e:
        return {"error": (await e.response.aread()).decode("utf-8", "replace")}
    return {"answer": "".join(answer),
            "citations": [{"doc_name": ci["doc_name"],
                           "heading_path": ci["heading_path"],
                           "snippet": ci["snippet"]} for ci in citations]}


def build_mcp(client: KBaseClient | None = None) -> FastMCP:
    mcp = FastMCP("kbase")
    c = client or KBaseClient(httpx.AsyncClient(base_url=DEFAULT_API))

    @mcp.tool()
    async def list_knowledge_bases() -> list | dict:
        """列出全部知识库（id 与名称）。"""
        return await list_knowledge_bases_impl(c)

    @mcp.tool()
    async def search_knowledge(kb_id: str, query: str, top_k: int = 5) -> list | dict:
        """在指定知识库中检索，返回带出处与相关度的原文块（不生成答案）。"""
        return await search_knowledge_impl(c, kb_id, query, top_k)

    @mcp.tool()
    async def ask_knowledge_base(kb_id: str, question: str,
                                 provider: str | None = None) -> dict:
        """对指定知识库完整 RAG 问答，返回答案与引用。"""
        return await ask_knowledge_base_impl(c, kb_id, question, provider)

    return mcp
```

注意：SSE 行解析此处按 httpx `aiter_lines`（已处理换行差异）；多 data 行 token 的换行拼接语义与前端一致的问题——后端 token 事件一段一 data 行（sse-starlette 对含 \n 的 token 会拆多行），此处 join("") 会丢换行：**实现时按事件收集 dataLines 并以 \n join，事件边界为空行**（比照 web-app/src/lib/sse.ts 的 accumulate-flush 逻辑移植为 Python）。测试补一条多行 token 事件用例（FakeLLM 输出含 \n 的 token）。

- [ ] Step 4 绿 + 全量回归（backend 107+5=~112；`pip install -e ".[dev,mcp]"` 先装 SDK）。Step 5 Commit `feat: kbase_mcp 三工具（HTTP 反调+SSE 组装+清晰错误面）`

---

### Task E2: 传输层与真实验收

**Files:** Create `kbase_mcp/__main__.py`；Test `tests/test_mcp_transport.py`

- `__main__.py`：argparse `--http --port 3001 --host 127.0.0.1`；STDIO 默认 `mcp.run()`；HTTP 用 FastMCP streamable-http（SDK 内置），`KBASE_MCP_TOKEN` 设置时注册中间件校验 `Authorization: Bearer <token>`（401 否则）。
- 测试：STDIO 进程内握手——用 mcp SDK 的内存客户端（`mcp.shared.memory` 或 stdio 子进程 spawn `python -m kbase_mcp` 环境变量指向 ASGI 不可行——子进程需真 URL；故 STDIO 测试用 SDK client_session 与 build_mcp 的内存连接，工具列表含三工具、调用 list 返回错误面【API 未启动也算通——错误面即契约】）；HTTP token 校验测试（起 uvicorn 内存不便——改为直接测中间件函数逻辑 + 手动验收）。
- **真实验收**：起真实 KBase（8100，彩排库）→ 子进程 spawn STDIO MCP → SDK ClientSession 实调三工具：list 含彩排库；search "住宿标准" 命中 xlsx；ask "出差北京住宿费标准是多少？"（真实 provider）answer 非空含 citations。留存输出摘录。
- Commit `feat: MCP 传输层（STDIO/HTTP+Bearer）与真实验收`

---

### Task E3: 文档与搭车项

- README 新增 "MCP Server" 章节：安装 `pip install -e ".[mcp]"`、启动两种传输、Claude Code 配置 JSON 示例（`claude mcp add kbase -- python -m kbase_mcp`）、Claude Desktop mcpServers 片段、KBASE_API_URL/KBASE_MCP_TOKEN 说明。
- 搭车：`eval/run_eval.py` Generator 构造改为 rerank 感知（与 main.py:276 相同逻辑：tiers 的 rerank 档与非 rerank 档各用对应 min_score；顺带传 min_include_score）。
- 全量回归；Commit `docs: MCP 使用文档 + eval 阈值与生产对齐`

## 任务依赖
E1 → E2 → E3
