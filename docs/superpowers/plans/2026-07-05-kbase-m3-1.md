# KBase M3-1（QueryRewrite+债务批次）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 多轮查询改写（三态模式）解决指代性追问拒答；清理 D1-D6 六项债务。

**Architecture:** 新组件 QueryRewriter（触发判定纯函数 + LLM 改写 + 失败回退），仅接入会话查询链路；债务项分两批落地（API/前端一批、摄取/检索加固一批）。

**Tech Stack:** 既有栈，零新依赖。

**Spec:** `docs/superpowers/specs/2026-07-05-kbase-m3-1-design.md`
**基线：** main @ 2a3902f（后端 85 passed/3 deselected；前端 31 passed）。分支 `feature/m3-1`。
**约定沿 M1/M2**：`.venv\Scripts\python`；PS5.1 无 `&&`；`.env` 用 `-Encoding utf8` 读；每任务一提交；前端改动须 `npm run build` 重建 web/ 一并入库。

---

### Task C1: QueryRewriter 组件（TDD）

**Files:** Create `kbase/rag/rewriter.py`；Modify `kbase/config.py`；Test `tests/test_rewriter.py`

- [ ] Step 1 失败测试：

```python
# tests/test_rewriter.py
import pytest

from kbase.rag.rewriter import QueryRewriter, RewriteResult, should_rewrite

HIST = [{"role": "user", "content": "出差北京住宿费标准是多少？"},
        {"role": "assistant", "content": "部级1100元，司局级650元，其他人员500元。"}]


class FakeLLM:
    def __init__(self, out="出差北京司局级住宿费标准是多少"):
        self.out = out
        self.calls = 0

    async def complete(self, messages, **params):
        self.calls += 1
        return self.out


class BrokenLLM:
    async def complete(self, messages, **params):
        raise RuntimeError("down")


def test_should_rewrite_conditional():
    assert should_rewrite("那司局级呢？", HIST, mode="conditional") is True   # 短+指代
    assert should_rewrite("那司局级呢？", [], mode="conditional") is False     # 无历史
    long_self = "兵团本级机关事业单位工作人员差旅费管理办法中关于住宿费报销的具体标准是什么"
    assert should_rewrite(long_self, HIST, mode="conditional") is False       # 长且与历史有重叠
    assert should_rewrite("公务卡结算范围", HIST, mode="conditional") is True  # 无指代但与历史无重叠且短


def test_should_rewrite_modes():
    assert should_rewrite("那呢", HIST, mode="off") is False
    assert should_rewrite(long_q := "一个完全自包含的很长很长的政策问题" * 3, HIST, mode="always") is True
    assert should_rewrite(long_q, [], mode="always") is False                 # always 也要求有历史


async def test_rewrite_triggered_uses_llm():
    llm = FakeLLM()
    r = QueryRewriter(llm=llm, mode="conditional")
    res = await r.rewrite("那司局级呢？", HIST)
    assert res.triggered and res.rewritten
    assert res.query == "出差北京司局级住宿费标准是多少"
    assert llm.calls == 1


async def test_rewrite_not_triggered_passthrough():
    llm = FakeLLM()
    r = QueryRewriter(llm=llm, mode="off")
    res = await r.rewrite("那司局级呢？", HIST)
    assert res.query == "那司局级呢？" and not res.triggered and llm.calls == 0


async def test_rewrite_failure_falls_back():
    r = QueryRewriter(llm=BrokenLLM(), mode="always")
    res = await r.rewrite("那司局级呢？", HIST)
    assert res.query == "那司局级呢？" and res.triggered and not res.rewritten


async def test_rewrite_empty_output_falls_back():
    r = QueryRewriter(llm=FakeLLM(out="  "), mode="always")
    res = await r.rewrite("那司局级呢？", HIST)
    assert res.query == "那司局级呢？" and not res.rewritten
```

- [ ] Step 2 红。Step 3 实现：

```python
# kbase/rag/rewriter.py
"""多轮查询改写：把依赖上下文的追问改写为自包含检索问题。
改写只影响检索输入；用户原文的存库与展示不变。失败/超时一律回退原问题。"""
import asyncio
import logging
from dataclasses import dataclass

logger = logging.getLogger(__name__)

DEFAULT_PRONOUNS = ["那", "这", "它", "他", "她", "其", "该", "上述", "前述",
                    "前面", "刚才", "呢", "些", "者"]
_SHORT_LEN = 20

_PROMPT = (
    "以下是一段对话历史：\n{history}\n\n"
    "用户当前的问题是：{question}\n\n"
    "请结合对话历史，把当前问题改写为一个不依赖上下文、语义完整的检索问题。"
    "保留原意，不要扩展无关内容。只输出改写后的问题，不要任何前缀或解释。"
)


def should_rewrite(question: str, history: list[dict], mode: str = "conditional",
                   pronouns: list[str] | None = None) -> bool:
    if mode == "off" or not history:
        return False
    if mode == "always":
        return True
    q = question.strip()
    if len(q) < _SHORT_LEN:
        return True
    if any(p in q for p in (pronouns or DEFAULT_PRONOUNS)):
        return True
    hist_text = " ".join(m["content"] for m in history)
    overlap = sum(1 for ch in set(q) if ch in hist_text)
    return overlap < len(set(q)) * 0.3      # 与历史几乎无字符重叠 → 疑似换话题的省略句


@dataclass
class RewriteResult:
    query: str          # 实际用于检索的问题
    triggered: bool     # 是否命中触发条件
    rewritten: bool     # LLM 改写是否成功生效


class QueryRewriter:
    def __init__(self, llm, mode: str = "conditional",
                 max_wait_s: float = 5.0, pronouns: list[str] | None = None):
        self._llm = llm
        self._mode = mode
        self._max_wait = max_wait_s
        self._pronouns = pronouns

    async def rewrite(self, question: str, history: list[dict]) -> RewriteResult:
        if not should_rewrite(question, history, self._mode, self._pronouns):
            return RewriteResult(query=question, triggered=False, rewritten=False)
        try:
            hist = "\n".join(f"{m['role']}: {m['content']}" for m in history)
            out = await asyncio.wait_for(
                self._llm.complete([{"role": "user", "content":
                                     _PROMPT.format(history=hist, question=question)}]),
                timeout=self._max_wait)
            out = (out or "").strip()
            if out:
                return RewriteResult(query=out, triggered=True, rewritten=True)
        except Exception as e:  # noqa: BLE001 —— 改写永不阻塞主链路
            logger.warning("查询改写失败，回退原问题: %s", e)
        return RewriteResult(query=question, triggered=True, rewritten=False)
```

config.py 追加 `class RewriteConfig(BaseModel): mode: str = "conditional"; provider: str | None = None; max_wait_s: float = 5.0`，`RetrievalConfig` 挂 `rewrite: RewriteConfig`，另加 `max_parent_chars: int = 4000`（D6 用）。

- [ ] Step 4 绿（7 passed）+ 全量回归。Step 5 Commit `feat: QueryRewriter（三态触发+LLM改写+失败回退）`

---

### Task C2: 接入会话查询链路

**Files:** Modify `kbase/api/main.py`；Test `tests/test_conversations.py`（追加）

- 组装：create_app 构建 `QueryRewriter`（llm 用 rewrite.provider 或 active 的懒解析，同 LazyEnricher 模式——`rewriter=None|False|instance` 测试注入参数）；conversations query 路由：`history = build_history(...)`（既有）→ `res = await rewriter.rewrite(question, history)` → `_run_query` 检索用 `res.query`，生成与落库仍用原 question。
- 追加测试（tests/test_conversations.py）：注入 FakeRewriteLLM（complete 返回改写句），第二轮发短追问，断言 SpyEmbedder（检索路）收到的 embed 文本含改写后的关键词而生成 llm 收到的 user 消息仍是原短问；另一条：rewriter=False 时行为与 M2 完全一致（既有测试不动即为证明）。
- 全量回归；Commit `feat: 会话查询接入 QueryRewrite（检索用改写、展示存库用原文）`

---

### Task C3: D1 删除知识库 + D2 会话分页（后端+前端）

**后端：**
- `DELETE /api/kb/{kb_id}`：级联顺序 store.delete_collection(kb_id)（ChromaStore 新增该方法，不存在集合容错）→ keyword_index.delete_kb → 该库全部 doc 的 files 目录 → chunks/documents 行 → 该库 conversations+messages 行 → kb 行；404 未知库。测试：删除后 chroma 集合查询为空、fts 空、会话空、二次删除 404。
- `GET /api/conversations` 加 `limit`（默认 30，上限 100）与 `offset`；响应改为 `{"items": [...], "total": n}`（**破坏性变更**：api.ts 同步改，旧前端已被 M2 替换无兼容负担）。测试：造 35 条会话分页取两页。

**前端：** KB 卡片右上删除图标按钮 → 确认 Dialog（显示"将删除 N 篇文档与相关会话，不可恢复"）→ deleteKb → 刷新列表 + toast；会话侧栏底部"加载更多"（items<total 时显示），点击 offset+=30 追加。api.ts：deleteKb、listConvs 分页签名。vitest：分页累加纯函数或组件逻辑 1 条。
- 全量回归（后端 +2~3，前端 +1）；`npm run build`；Commit `feat: 删除知识库级联+会话分页（D1/D2）`

---

### Task C4: D3/D4/D5/D6 加固批次

- **D3**：`retry_kb_ocr` 改为收集 pending_ocr 文档 id 列表后 `bg.add_task(一个顺序处理函数)`，响应 `{"queued": n}`。测试：3 个 pending 文档一次批量重试全部转 ready（FakeOCR 恢复可用），且处理函数是单入口（monkeypatch 计数 add_task 恰 1 次）。
- **D4**：migrations 追加 `CREATE UNIQUE INDEX IF NOT EXISTS uq_doc_kb_hash ON documents(kb_id, content_hash)`；注意既有库若有历史重复行需先清（迁移函数里先删重复保留最早一条再建索引——写成幂等）。pipeline.ingest_file 捕获 IntegrityError → 重查返回既有 id。测试：并发同内容摄取（两线程同一文件）最终 documents 表恰一行。
- **D5**：api 上传路由改为收集 (dest, safe_name) 列表后单 bg task：`ThreadPoolExecutor(max_workers=cfg.ingest.workers)` map ingest_file。config 加 `class IngestConfig(BaseModel): workers: int = 2`。测试：上传 3 文件全部 ready（TestClient bg 同步语义仍成立——executor 在 task 内同步 shutdown(wait=True)）。
- **D6**：retriever._assemble 父块超 `max_parent_chars` 时以命中叶子文本首次出现位置为中心截窗（找不到叶子文本则截头部），前后加 `…`；Retriever 构造参数 `max_parent_chars=4000`，api 组装传 cfg 值。测试：构造 6000 字父块 + 命中叶子在尾部，断言返回 text 长度 ≤ 上限+少量标记、含叶子文本、以 … 开头。
- 全量回归；Commit `feat: OCR批量单工位+去重约束+摄取并行+父块截窗（D3-D6）`

---

### Task C5: 真实验收与收尾

1. 起真实后端（8100，data/ 彩排库），多轮会话端到端：问"出差北京住宿费标准是多少？"→ 追问"那司局级呢？"——断言第二问 SSE 有 citations 且答案含 650；日志确认 rewrite 触发与改写文本。用脚本走 SSE（同 M1 T15 方式）。
2. `mode: off` 配置临时验证第二问回到拒答（证明改写是因果）；恢复 conditional。
3. README：多轮改写一段说明（mode 三态配置）；删除知识库与分页无需文档。
4. 后端+前端全量回归；`git status` 干净。
5. Commit `docs: README 查询改写配置说明` + 验收报告。

## 任务依赖
C1 → C2 → C3 → C4 → C5（顺序，C3/C4 无耦合但共享 api/main.py 保持串行）
